# Team photos

Drop the two founder headshots here so the Founder / team section shows real
faces instead of the styled initials placeholder:

- `founder.jpg` — your partner (the builder)
- `cofounder.jpg` — you (head of sales & marketing)

Square images look best (they're rendered in a 1:1 frame). JPG or PNG both work;
if you use PNG, either rename to `.jpg`-agnostic paths or update the `photo`
paths in `lib/site.ts`.

Also update the names/roles/notes in `lib/site.ts` (`team` array) — they're
placeholders right now.

> Note: your attached headshot couldn't be saved automatically from the build
> session (it came through as a chat image, not a file). Save it here as
> `cofounder.jpg`. Your partner's photo lives on the current mente-ai.com site,
> which was blocked from this build environment — grab it from there and save it
> as `founder.jpg`.
